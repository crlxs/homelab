services:
  prowlarr:
    image: lscr.io/linuxserver/prowlarr:latest
    container_name: prowlarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${SERVARR_CONF_DIR}/prowlarr:/config
    ports:
      - 9696:9696
    restart: unless-stopped
  
  radarr:
    image: lscr.io/linuxserver/radarr:latest
    container_name: radarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${SERVARR_CONF_DIR}/radarr:/config
      - ${MEDIA_DIR}:${MEDIA_DIR} # By making the internal path match the external one we avoid folder errors with SABnzbd.
    ports:
      - 7878:7878
    restart: unless-stopped

  sonarr:
    image: lscr.io/linuxserver/sonarr:latest
    container_name: sonarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${SERVARR_CONF_DIR}/sonarr:/config
      - ${MEDIA_DIR}:${MEDIA_DIR}
    ports:
      - 8989:8989
    restart: unless-stopped

#  lidarr:
#    image: lscr.io/linuxserver/lidarr:latest
#    container_name: lidarr
#    environment:
#      - PUID=${PUID}
#      - PGID=${PGID}
#      - TZ=${TZ}
#    volumes:
#      - ${SERVARR_CONF_DIR}/lidarr:/config
#    ports:
#      - 8686:8686
#    restart: unless-stopped

  jellyseerr:
    image: ghcr.io/fallenbagel/jellyseerr:latest
    init: true
    container_name: jellyseerr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${SERVARR_CONF_DIR}/jellyseerr:/app/config
    ports:
      - 5055:5055
    restart: unless-stopped

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest # Use a specific version for stability
    container_name: cadvisor
    ports:
      - 8080:8080
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    devices:
      - /dev/kmsg
    restart: unless-stopped
    privileged: true # Often required on modern kernels to access cgroup data

   # Add as a sidecar to radarr to export specific radarr info like movies, etc. 
#  radarr-exporter:
#    image: ghcr.io/onedr0p/exportarr:latest
#    container_name: radarr-exporter
#    command: radarr
#    environment:
#      - URL=http://radarr:7878
#      - APIKEY=${RADARR_API_KEY}
#    ports:
#      - 9793:9793
